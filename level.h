#include <iostream>
#include <utility>
#include <vector>
#include "common.h"
#include "clstuff.h"

namespace parser
{
void save( const char* filename, RTContext& context );
void load( const char* filename, RTContext& context );
} // namespace parser